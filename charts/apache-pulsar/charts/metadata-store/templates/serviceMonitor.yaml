{{- if (eq (include "metadata-store.metrics-enabled" .) "true") }}
  {{- $component := "metadata-store" -}}
  {{- $name := (printf "%s-service-monitor" (include "metadata-store.name" $)) -}}

apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ $name | quote }}
  namespace: {{ (include "common.names.namespace" .) | quote }}
  labels: {{ include "common.labels.standard" (dict "name" (include "metadata-store.name" .) "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: {{ $component | quote }}
    service_monitor: "1"
    {{ include "common.tplvalues.render" ( dict "value" .Values.global.commonLabels "context" $ ) | nindent 4 }}
  annotations:
    {{ include "common.tplvalues.render" ( dict "value" .Values.global.commonAnnotations "context" $ ) | nindent 4 }}
spec:
  selector:
    matchLabels: {{ (include "common.labels.matchLabels" (dict "name" (include "metadata-store.name" $) "context" $ )) | nindent 6 }}
      app.kubernetes.io/component: {{ $component | quote }}
  endpoints:
    - port: metrics
      path: /metrics
{{- end -}}