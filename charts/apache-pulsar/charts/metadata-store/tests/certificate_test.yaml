suite: Test metadata-store certificate
release:
  name: zz
  namespace: xx
templates:
  - charts/metadataStore/templates/certificate.yaml
  - charts/metadataStore/templates/jks_secret.yaml
tests:
  - it: should be correct certificate document
    set:
      global:
        interComponentTls:
          enabled: true
      metadataStore:
        certificateResources:
          tlsSecretName: null
          jksPasswordSecretName: null
          certificateRequest:
            issuerRef:
              name: some-issuer
              kind: ClusterIssuer
    asserts:
      - containsDocument:
          kind: Certificate
          apiVersion: cert-manager.io/v1
    template: charts/metadataStore/templates/certificate.yaml

  - it: should be correct secret document
    set:
      global:
        interComponentTls:
          enabled: true
      metadataStore:
        certificateResources:
          tlsSecretName: null
          jksPasswordSecretName: null
          certificateRequest:
            issuerRef:
              name: some-issuer
              kind: ClusterIssuer
    asserts:
      - containsDocument:
          kind: Secret
          apiVersion: v1
    template: charts/metadataStore/templates/jks_secret.yaml

  - it: should have correct certificate metadata
    set:
      global:
        commonLabels:
          aaa: bbb
        commonAnnotations:
          ccc: ddd
        interComponentTls:
          enabled: true
      metadataStore:
        certificateResources:
          tlsSecretName: null
          jksPasswordSecretName: null
          certificateRequest:
            issuerRef:
              name: some-issuer
              kind: ClusterIssuer
    asserts:
      - equal:
          path: metadata
          value:
            annotations:
              ccc: ddd
            labels:
              aaa: bbb
              app.kubernetes.io/component: metadata-store
              app.kubernetes.io/instance: zz
              app.kubernetes.io/managed-by: Helm
              app.kubernetes.io/name: metadata-store
              app.kubernetes.io/part-of: apache-pulsar
              helm.sh/chart: metadataStore-0.1.0
            name: metadata-store-certificate
            namespace: xx
    template: charts/metadataStore/templates/certificate.yaml

  - it: should have correct secret metadata
    set:
      global:
        commonLabels:
          aaa: bbb
        commonAnnotations:
          ccc: ddd
        interComponentTls:
          enabled: true
      metadataStore:
        certificateResources:
          tlsSecretName: null
          jksPasswordSecretName: null
          certificateRequest:
            issuerRef:
              name: some-issuer
              kind: ClusterIssuer
      asserts:
        - equal:
            path: metadata
            value:
              annotations:
                ccc: ddd
              labels:
                aaa: bbb
                app.kubernetes.io/component: metadata-store
                app.kubernetes.io/instance: zz
                app.kubernetes.io/managed-by: Helm
                app.kubernetes.io/name: metadata-store
                app.kubernetes.io/part-of: pulsar-luna
                helm.sh/chart: metadataStore-0.1.0
              name: metadata-store-jks
              namespace: xx
      template: charts/metadataStore/templates/jks_secret.yaml

  - it: should build correct certificate request
    template: charts/metadataStore/templates/certificate.yaml
    set:
      global:
        clusterDomain: domain.com
        interComponentTls:
          enabled: true
      metadataStore:
        certificateResources:
          tlsSecretName: null
          jksPasswordSecretName: null
          certificateRequest:
            issuerRef:
              name: cert-issuer
              kind: ClusterIssuer
    asserts:
      - equal:
          path: spec.commonName
          value: zz-metadata-store
      - contains:
          path: spec.dnsNames
          content:
            '*.metadata-store-headless.xx.svc.domain.com'
      - contains:
          path: spec.dnsNames
          content:
            127.0.0.1
      - contains:
          path: spec.dnsNames
          content:
            localhost
      - equal:
          path: spec.secretName
          value: metadata-store-tls

  - it: should build correct certificate request with global values
    template: charts/metadataStore/templates/certificate.yaml
    set:
      global:
        clusterDomain: domain.com
        interComponentTls:
          enabled: true
          tlsSecretName: null
          jksPasswordSecretName: null
          issuerRef:
            name: cluster-cert-issuer
            kind: ClusterIssuer
      metadataStore:
        certificateResources:
          tlsSecretName: null
          jksPasswordSecretName: null
          certificateRequest:
            issuerRef:
    asserts:
      - equal:
          path: spec.commonName
          value: zz-metadata-store
      - contains:
          path: spec.dnsNames
          content:
            '*.metadata-store-headless.xx.svc.domain.com'
      - contains:
          path: spec.dnsNames
          content:
            127.0.0.1
      - contains:
          path: spec.dnsNames
          content:
            localhost
      - equal:
          path: spec.secretName
          value: metadata-store-tls
      - equal:
          path: spec.issuerRef
          value:
            name: cluster-cert-issuer
            kind: ClusterIssuer